{
  "name": "reasons",
  "version": "0.1.0",
  "description": "Git-native annotations that pin the *why* to the code it explains, and surface it to humans and agents when they touch that code.",
  "type": "module",
  "bin": { "reasons": "./dist/cli.js" },
  "scripts": {
    "build": "tsc",
    "dev": "tsx src/cli.ts",
    "test": "tsx --test src/*.test.ts"
  },
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^24.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  }
}
